{
  "name": "@illinois-toolkit/ilw-hero",
  "description": "Illinois Toolkit:  The Hero Image component adds a large picture at the top of the page.",
  "private": false,
  "version": "1.0.1-beta",
  "type": "module",
  "files": [
    "src/**",
    "dist/**",
    "builder/**"
  ],
  "exports": {
    ".": {
      "import": "./src/ilw-hero.js",
      "require": "./src/ilw-hero.cjs",
      "default": "./src/ilw-hero.js"
    }
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build --config vite.build.config.js --emptyOutDir",
    "preview": "vite preview",
    "prepack": "npm run build"
  },
  "dependencies": {
    "lit": "3.1.3"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
